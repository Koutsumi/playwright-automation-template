import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"PlayPattern","description":"","frontmatter":{},"headers":[],"relativePath":"index.md","filePath":"index.md"}');
const _sfc_main = { name: "index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="playpattern" tabindex="-1">PlayPattern <a class="header-anchor" href="#playpattern" aria-label="Permalink to “PlayPattern”">​</a></h1><h2 id="visao-geral" tabindex="-1">Visão Geral <a class="header-anchor" href="#visao-geral" aria-label="Permalink to “Visão Geral”">​</a></h2><p>Este repositório passa a ser a base de referência para projetos de automação com Playwright na empresa.</p><p>O objetivo da documentação é deixar claros:</p><ul><li>o padrão de estrutura do projeto;</li><li>a separação entre testes <code>api</code> e <code>e2e</code>;</li><li>a organização por módulos;</li><li>os pontos de extensão que variam entre projetos;</li><li>a convenção para integrações com banco de dados em <code>shared/database</code>.</li></ul><h2 id="o-que-este-template-resolve" tabindex="-1">O que este template resolve <a class="header-anchor" href="#o-que-este-template-resolve" aria-label="Permalink to “O que este template resolve”">​</a></h2><ul><li>acelera a criação de novos projetos de automação;</li><li>reduz divergência entre times;</li><li>facilita onboarding;</li><li>padroniza setup, naming e organização dos testes;</li><li>centraliza exemplos de integração com recursos compartilhados.</li></ul><h2 id="pilares-do-template" tabindex="-1">Pilares do template <a class="header-anchor" href="#pilares-do-template" aria-label="Permalink to “Pilares do template”">​</a></h2><ul><li><code>Playwright</code> como executor principal de testes.</li><li><code>TypeScript</code> para tipagem e reuso.</li><li>Estrutura modular para API e E2E.</li><li><code>shared/</code> para utilitários comuns entre módulos.</li><li>Banco de dados tratado como detalhe de projeto, mas sempre configurado em <code>shared/database</code>.</li><li><code>Page Objects</code> com uma classe por página para os testes E2E.</li><li><code>fixtures</code> centralizados para usuários e massas reutilizáveis.</li><li><code>auth.setup.ts</code> como ponto de preparação lógica de usuários e sessão.</li></ul><h2 id="quando-adaptar" tabindex="-1">Quando adaptar <a class="header-anchor" href="#quando-adaptar" aria-label="Permalink to “Quando adaptar”">​</a></h2><p>Cada projeto pode mudar:</p><ul><li>módulos disponíveis;</li><li>variáveis de ambiente;</li><li>massa de dados;</li><li>estratégia de autenticação;</li><li>banco utilizado.</li></ul><p>O que não deve mudar sem necessidade é a organização base do template. Isso mantém previsibilidade entre repositórios e reduz custo de manutenção.</p><h2 id="o-que-ja-esta-exemplificado-neste-template" tabindex="-1">O que já está exemplificado neste template <a class="header-anchor" href="#o-que-ja-esta-exemplificado-neste-template" aria-label="Permalink to “O que já está exemplificado neste template”">​</a></h2><ul><li>módulo <code>auth</code> como referência mínima;</li><li>testes <code>success</code> e <code>errors</code> para API e E2E;</li><li>padrão de escrita com <code>Arrange</code>, <code>Act</code> e <code>Assert</code>;</li><li><code>Page Object</code> de login em <code>pages/auth/login.page.ts</code>;</li><li><code>fixtures/users.ts</code> inspirado no repositório <code>padrao</code>;</li><li>estrutura de setup com comentários explicando preparo via banco e persistência de cookies.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
