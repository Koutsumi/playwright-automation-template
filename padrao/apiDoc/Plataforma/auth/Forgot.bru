meta {
  name: Forgot
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/forgot
  body: json
  auth: inherit
}

headers {
  Origin: {{BASE_URL}}
}

body:json {
  {
    "email": "{{user_email}}"
  }
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
