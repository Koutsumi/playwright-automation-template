export enum EPersonType {
    legalPerson = 'LegalPerson',
    naturalPerson = 'NaturalPerson',
    cooperative = 'Cooperative',
    collective = 'Collective'
}

export enum EProponentStatus {
	INVALID = 'INVALID',
	VALIDATED = 'VALIDATED',
	DELETED = 'DELETED',
}